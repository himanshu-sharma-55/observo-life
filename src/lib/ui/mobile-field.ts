/** 16px on mobile avoids iOS focus/keyboard quirks; smaller from `sm` up. */
export const mobileFieldTextClass = "text-base sm:text-sm";

/** Toolbar filters: taller tap targets on mobile. */
export const mobileCompactFieldClass = "h-10 text-base sm:h-8 sm:text-sm";
